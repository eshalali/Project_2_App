{% layout 'layout.liquid' %}
{% block content %}
<div class="container-md  show">
    <h2>Expense</h2>
    <h5>Name: {{item.name}}</h5>
    <h6>Date: {{item.date}}</h6>
    <h6>Amount: ${{item.amount}}</h6>
    <h6>Description: {{item.description}}</p>
    <h6>Category: {{item.category}}</h6>
        <div>
        <a href="/expected/{{item._id}}/edit" class="btn btn-primary">Edit expense</a>
        </div>
        <div>
            <form action="/expected/{{item._id}}?_method=DELETE" method="POST">
                <input type="submit" value="Delete expense" class="btn btn-default">
            </form>
        </div>
    </div>
</div>

{% endblock %}